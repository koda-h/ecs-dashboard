# テストケース一覧 — ユーザ管理機能 (add_manage_view)

## 対象テストファイル

| ファイル | 種別 | ケース数 |
|---|---|---|
| `__tests__/auth/guard.test.ts` | 更新 | +14 |
| `__tests__/users/role.test.ts` | 新規 | 22 |
| `__tests__/users/permission.test.ts` | 新規 | 51 |

---

## `__tests__/auth/guard.test.ts` — 追記分

### 管理者専用ルートの分類

| # | テストケース |
|---|---|
| 1 | ユーザ一覧ページ(/users)は管理者専用ルートとして分類されること |
| 2 | ユーザ詳細ページ(/users/some-id)は管理者専用ルートとして分類されること |
| 3 | ユーザ詳細ページの動的セグメント(/users/[id])を含むパスは管理者専用ルートとして分類されること |
| 4 | サービス一覧ページ(/)は管理者専用ルートではないこと |
| 5 | ログインページ(/login)は管理者専用ルートではないこと |
| 6 | ユーザ登録ページ(/register)は管理者専用ルートではないこと |

### ロールを考慮したリダイレクト判定

| # | テストケース |
|---|---|
| 7 | 未認証状態でユーザ一覧ページ(/users)にアクセスした場合、ログインページへのリダイレクトが必要と判定されること |
| 8 | 未認証状態でユーザ詳細ページ(/users/some-id)にアクセスした場合、ログインページへのリダイレクトが必要と判定されること |
| 9 | Viewerロールのユーザがユーザ一覧ページ(/users)にアクセスした場合、サービス一覧ページへのリダイレクトが必要と判定されること |
| 10 | Viewerロールのユーザがユーザ詳細ページ(/users/some-id)にアクセスした場合、サービス一覧ページへのリダイレクトが必要と判定されること |
| 11 | Editorロールのユーザがユーザ一覧ページ(/users)にアクセスした場合、サービス一覧ページへのリダイレクトが必要と判定されること |
| 12 | Editorロールのユーザがユーザ詳細ページ(/users/some-id)にアクセスした場合、サービス一覧ページへのリダイレクトが必要と判定されること |
| 13 | Adminロールのユーザがユーザ一覧ページ(/users)にアクセスした場合、アクセスが許可と判定されること |
| 14 | Adminロールのユーザがユーザ詳細ページ(/users/some-id)にアクセスした場合、アクセスが許可と判定されること |
| 15 | Viewerロールの認証済みユーザがサービス一覧ページ(/)にアクセスした場合、アクセスが許可と判定されること |
| 16 | Editorロールの認証済みユーザがサービス一覧ページ(/)にアクセスした場合、アクセスが許可と判定されること |
| 17 | Adminロールの認証済みユーザがサービス一覧ページ(/)にアクセスした場合、アクセスが許可と判定されること |

---

## `__tests__/users/role.test.ts` — 新規

### ロールの有効性チェック

| # | テストケース |
|---|---|
| 1 | 'Admin' は有効なロールとして判定されること |
| 2 | 'Editor' は有効なロールとして判定されること |
| 3 | 'Viewer' は有効なロールとして判定されること |
| 4 | 空文字は無効なロールとして判定されること |
| 5 | 小文字表記('admin', 'editor', 'viewer')は無効なロールとして判定されること |
| 6 | 存在しないロール名('SuperAdmin', 'Guest' など)は無効なロールとして判定されること |
| 7 | null は無効なロールとして判定されること |
| 8 | undefined は無効なロールとして判定されること |
| 9 | 数値は無効なロールとして判定されること |

### ロール種別の判定 — isAdmin

| # | テストケース |
|---|---|
| 10 | 'Admin' ロールは Admin と判定されること |
| 11 | 'Editor' ロールは Admin ではないこと |
| 12 | 'Viewer' ロールは Admin ではないこと |

### ロール種別の判定 — isEditor

| # | テストケース |
|---|---|
| 13 | 'Editor' ロールは Editor と判定されること |
| 14 | 'Admin' ロールは Editor ではないこと |
| 15 | 'Viewer' ロールは Editor ではないこと |

### ロール種別の判定 — isViewer

| # | テストケース |
|---|---|
| 16 | 'Viewer' ロールは Viewer と判定されること |
| 17 | 'Admin' ロールは Viewer ではないこと |
| 18 | 'Editor' ロールは Viewer ではないこと |

### デフォルトロール

| # | テストケース |
|---|---|
| 19 | getDefaultRole() は 'Viewer' を返すこと |

### 自分自身の判定 — isSelf

| # | テストケース |
|---|---|
| 20 | 同じ内部IDを渡した場合、自分自身と判定されること |
| 21 | 異なる内部IDを渡した場合、自分自身ではないと判定されること |
| 22 | 片方が空文字の場合、自分自身ではないと判定されること |

### ロール変更の可否 — canChangeRole

| # | テストケース |
|---|---|
| 23 | Admin が別のユーザのロールを変更しようとした場合、許可と判定されること |
| 24 | Admin が自分自身のロールを変更しようとした場合、禁止と判定されること |

### ユーザ削除の可否 — canDeleteUser

| # | テストケース |
|---|---|
| 25 | Admin が別のユーザを削除しようとした場合、許可と判定されること |
| 26 | Admin が自分自身を削除しようとした場合、禁止と判定されること |

---

## `__tests__/users/permission.test.ts` — 新規

### filterServicesForPermission — クラスター名フィルタ

| # | テストケース |
|---|---|
| 1 | サービスリストが空の場合、空配列を返すこと |
| 2 | クラスター名フィルタが空文字の場合、全サービスを返すこと |
| 3 | クラスター名に部分一致するサービスのみを返すこと |
| 4 | クラスター名フィルタがどのサービスにも一致しない場合、空配列を返すこと |
| 5 | クラスター名フィルタは大文字小文字を区別しないこと |
| 6 | クラスター名フィルタに前後のスペースが含まれていても一致すること |

### filterServicesForPermission — サービス名フィルタ

| # | テストケース |
|---|---|
| 7 | サービス名フィルタが空文字の場合、全サービスを返すこと |
| 8 | サービス名に部分一致するサービスのみを返すこと |
| 9 | サービス名フィルタがどのサービスにも一致しない場合、空配列を返すこと |
| 10 | サービス名フィルタは大文字小文字を区別しないこと |
| 11 | サービス名フィルタに前後のスペースが含まれていても一致すること |

### filterServicesForPermission — クラスター名とサービス名の複合フィルタ

| # | テストケース |
|---|---|
| 12 | クラスター名とサービス名の両方に一致するサービスのみを返すこと |
| 13 | クラスター名に一致してもサービス名に一致しない場合、空配列を返すこと |
| 14 | サービス名に一致してもクラスター名に一致しない場合、空配列を返すこと |
| 15 | 両フィルタが空文字の場合、全サービスを返すこと |

### filterServicesForPermission — 順序保持

| # | テストケース |
|---|---|
| 16 | フィルタ後の結果が元のリストの順序を保持すること |

### addServicePermissions — 基本追加

| # | テストケース |
|---|---|
| 17 | 操作可能サービス一覧が空の場合、追加したサービスが一覧に含まれること |
| 18 | 既存の一覧に新しいサービスを追加すると、そのサービスが一覧に追加されること |
| 19 | 複数のサービスを一度に追加できること |
| 20 | 追加後も既存のサービスがすべて残ること |
| 21 | 空のリストを追加しても一覧が変化しないこと |

### addServicePermissions — 重複排除

| # | テストケース |
|---|---|
| 22 | 既に一覧にあるサービスを再度追加しても一覧に1件しか含まれないこと |
| 23 | 追加リスト内に同一サービスが重複していても一覧に1件しか含まれないこと |
| 24 | 一部が既存と重複するリストを追加した場合、重複分は1件に統合されること |

### addServicePermissions — 順序

| # | テストケース |
|---|---|
| 25 | 追加前の既存サービスの順序が維持されること |
| 26 | 新しいサービスは既存サービスの後に追加されること |

### removeServicePermissions — 基本削除

| # | テストケース |
|---|---|
| 27 | 指定したサービスが一覧から削除されること |
| 28 | 複数のサービスを一度に削除できること |
| 29 | 削除後も残るべきサービスはすべて変化しないこと |

### removeServicePermissions — 境界条件

| # | テストケース |
|---|---|
| 30 | 存在しないサービスを削除しようとしても一覧が変化しないこと |
| 31 | 空の一覧から削除しようとしても空のまま返ること |
| 32 | 空の削除リストを渡しても一覧が変化しないこと |
| 33 | 一覧にある全サービスを削除すると空配列が返ること |

### removeServicePermissions — 順序

| # | テストケース |
|---|---|
| 34 | 削除後の残りサービスが元の順序を保持すること |

### canOperateService — Admin ロール

| # | テストケース |
|---|---|
| 35 | Admin ロールのユーザは権限リストが空でも任意のサービスを操作できること |
| 36 | Admin ロールのユーザは権限リストに含まれていないサービスも操作できること |

### canOperateService — Editor ロール

| # | テストケース |
|---|---|
| 37 | Editor ロールのユーザは権限リストに含まれているサービスを操作できること |
| 38 | Editor ロールのユーザは権限リストに含まれていないサービスを操作できないこと |
| 39 | Editor ロールのユーザで権限リストが空の場合、どのサービスも操作できないこと |

### canOperateService — Viewer ロール

| # | テストケース |
|---|---|
| 40 | Viewer ロールのユーザは権限リストに含まれているサービスでも操作できないこと |
| 41 | Viewer ロールのユーザは権限リストが空の場合、どのサービスも操作できないこと |

### canViewServices — 閲覧権限

| # | テストケース |
|---|---|
| 42 | Admin ロールのユーザはサービスを閲覧できること |
| 43 | Editor ロールのユーザはサービスを閲覧できること |
| 44 | Viewer ロールのユーザはサービスを閲覧できること |
